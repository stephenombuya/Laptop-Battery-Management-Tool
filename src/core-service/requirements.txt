# BatteryOS — Python Core Service Dependencies
# Install with: pip install -r requirements.txt

# Battery and system monitoring
psutil>=5.9.0

# Desktop notifications (Windows only — ignored on macOS/Linux)
win10toast>=0.9; sys_platform == "win32"

# Type stubs for static analysis (development only)
mypy>=1.0

# Linting and formatting (development only)
ruff>=0.4.0
